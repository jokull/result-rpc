---
import PageLayout from 'blume/components/layout/PageLayout.astro';
import data from 'blume:data';

const pageDescription = 'Typed RPC for React. Errors accumulate along the call path and discharge along the component tree.';

---

<PageLayout
  site={{ title: data.config.title, description: data.config.description }}
  logo={data.config.logo}
  favicon={data.config.favicon}
  appleIcon={data.config.appleIcon}
  banner={data.config.banner}
  analytics={data.config.analytics}
  navigation={data.navigation}
  themeMode={data.config.theme.mode}
  fontCssVars={data.fontCssVars}
  searchEnabled={data.config.search.enabled}
  siteUrl={data.config.site}
  ogEnabled={data.config.og.enabled}
  ogImage="/og-home.png"
  page={{
    title: 'result-rpc — typed RPC for React',
    description: pageDescription,
    route: '/',
  }}
>
  <header class="nav shell">
    <a class="brand" href="/" aria-label="result-rpc home">
      <img src="/result-rpc-lockup-blue.svg" alt="result-rpc" width="206" height="41" />
    </a>
    <nav aria-label="Main navigation">
      <a href="/start/introduction/">Docs</a>
      <a href="/concepts/shells/">Shells</a>
      <a href="/concepts/entities/">Entities</a>
      <a href="/guides/migrating-from-trpc/">From tRPC</a>
      <a class="demo-link" href="https://demo.result-rpc.com">Live demo <span>↗</span></a>
      <a class="github" href="https://github.com/jokull/result-rpc">GitHub <span>↗</span></a>
    </nav>
  </header>

  <div class="home-content">
    <section class="hero shell">
      <div class="hero-copy">
        <div class="kicker"><span></span> Typed RPC and failure ownership for React</div>
        <h1>Errors are values.<br /><em>Values have owners.</em></h1>
        <p class="lede">Errors accumulate along the call path and discharge along the component tree. Every operation has one closed, wire-safe failure union; React shells claim failures owned above a component, leaving that component with only the cases it must handle.</p>
        <div class="actions">
          <a class="button primary" href="/start/quickstart/">Read the quickstart <span>→</span></a>
          <a class="button secondary" href="/start/introduction/">Read the introduction</a>
          <a class="button secondary" href="https://demo.result-rpc.com">Open the live demo <span>↗</span></a>
        </div>
      </div>
      <div class="hero-visual">
        <figure class="hero-code" aria-label="The same call, unnarrowed and under the shells">
          <pre><code><span class="c">// the complete operation union</span>
<span class="k">const</span> query = useResultQuery(client.doc.byId, &#123; id &#125;)
<span class="c">// DocNotFound | Unauthorized | ServerInternal | Offline |
// NetworkFailure | Timeout | HttpFailure |
// ProtocolViolation | DecodeFailure | Stale</span>

<span class="c">// enclosing shells subtract the failures they own</span>
<span class="k">const</span> query = AuthShell.useQuery(client.doc.byId, &#123; id &#125;)
<span class="c">// DocNotFound — and a missing case fails to compile</span></code></pre>
          <figcaption>The full union remains on the operation. This component sees a narrower union because mounted shells own the other tags.</figcaption>
        </figure>
      </div>
    </section>

    <section class="pillars shell" aria-labelledby="pillars-title">
      <div class="section-heading">
        <p class="section-label">System model</p>
        <h2 id="pillars-title">How an operation stays typed end to end.</h2>
      </div>

      <article class="pillar">
        <div class="pillar-copy">
          <p class="eyebrow">01 · Accumulation and discharge</p>
          <h3>Failure ownership follows the component tree.</h3>
          <p><strong>Expected failures are part of the contract. Unexpected exceptions are not.</strong> Procedures return anticipated failures as tagged values. Unexpected server exceptions are reported through server-only observability with their private cause and exposed to the client only as a sanitized <code>server/internal</code> failure.</p>
          <p>Procedure and middleware failures accumulate with server and client boundary failures to form the operation's closed union.</p>
          <p>A shell claims tags at a position in the React tree. Shells form an explicit parent chain, so a derived shell carries every claim made above it. Its hooks subtract those tags, leaving the component with only its remaining failures; unclaimed failures stay in the union and must be handled.</p>
          <a class="pillar-link" href="/concepts/shells/">How shells claim failures <span>→</span></a>
        </div>
        <figure class="pillar-code">
          <pre><code><span class="c">// SERVER — the layer proves write access</span>
<span class="k">const</span> rename = app.procedure()
  .use(requireWriteAccess)
  <span class="c">// context.writer + WriteAccessRequired</span>
  .errors(&#123; DocLocked &#125;)
  .mutation((&#123; context, input &#125;) =&gt;
    context.docs.rename(input, context.writer))

<span class="c">// REACT — the shell owns the access failure</span>
<span class="k">const</span> rename = WriteAccessShell.useMutation(
  client.doc.rename,
)
<span class="c">// this component handles DocLocked</span></code></pre>
        </figure>
      </article>

      <article class="pillar">
        <div class="pillar-copy">
          <p class="eyebrow">02 · The wire contract</p>
          <h3>Values are validated at the wire.</h3>
          <p>Codecs validate inputs, outputs, and error data on both sides. They also encode <code>Date</code>, <code>Map</code>, <code>Set</code>, <code>BigInt</code>, cycles, and repeated references without changing the application type.</p>
          <p>Models add identity where it is useful. A model projection can sit inside a one-off output shape without a global graph schema or selection set, and the client can patch matching model instances by key.</p>
          <a class="pillar-link" href="/concepts/wire/">Rich types on the wire <span>→</span></a>
        </div>
        <figure class="pillar-code">
          <pre><code><span class="k">const</span> search = app.procedure()
  .output(wire.object(&#123;
    hotel:  Hotel.pick(<span class="s">"id"</span>, <span class="s">"name"</span>), <span class="c">// entity — patches by id</span>
    openAt: wire.date,                <span class="c">// a real Date, not a string</span>
    from:   wire.number,              <span class="c">// a one-off aggregate</span>
  &#125;))
  .query(handler)

<span class="c">// client receives: &#123; hotel, openAt: Date, from &#125;</span>
<span class="c">// clean types. no codegen. no selection set.</span></code></pre>
        </figure>
      </article>

      <article class="pillar">
        <div class="pillar-copy">
          <p class="eyebrow">03 · The client boundary</p>
          <h3>The contract exists at runtime.</h3>
          <p>The client consumes a runtime contract containing codecs, error definitions, and policies. That runtime value is what validates and reconstructs rich values and tagged errors after they cross the wire.</p>
          <p>Shared contract modules remain free of server implementation code. The browser client imports that contract; handlers, database drivers, and environment access stay behind the server entry.</p>
          <a class="pillar-link" href="/concepts/client-boundary/">The client boundary rule <span>→</span></a>
        </div>
        <figure class="pillar-code">
          <pre><code><span class="c">// ✅ build the client from the CONTRACT —</span>
<span class="c">//    a runtime of codecs, and nothing else</span>
<span class="k">import</span> &#123; appContract &#125; <span class="k">from</span> <span class="s">"./contract"</span>
<span class="k">export const</span> client = createBrowserClient(&#123;
  contract: appContract, transport,
&#125;)

<span class="c">// Avoid importing the ROUTER into browser code:</span>
<span class="c">// it retains handlers and their server imports.</span></code></pre>
        </figure>
      </article>

      <article class="pillar">
        <div class="pillar-copy">
          <p class="eyebrow">04 · Client freshness</p>
          <h3>Mutation effects are declared once.</h3>
          <p>Returning a modeled entity patches matching cached instances by key. A mutation can use <code>.affects()</code> to declare query memberships that may change, while a handler can <code>touch()</code> identities affected by server-side cascades.</p>
          <p>These effects are declared with the mutation instead of repeated by each consumer. TanStack Query core remains the internal scheduler and cache engine for staleness, retries, refetching, and garbage collection.</p>
          <a class="pillar-link" href="/concepts/entities/">The entity cache <span>→</span></a>
        </div>
        <figure class="pillar-code">
          <pre><code><span class="c">// FREE — return the entity, every view patches by id</span>
rename: app.procedure()
  .output(User.pick(<span class="s">"id"</span>, <span class="s">"name"</span>)).mutation(handler)

<span class="c">// MEMBERSHIP — which lists change, declared once</span>
comment: app.procedure().affects(commentsFor).mutation(handler)

<span class="c">// CASCADES the output can't name — the server touches them</span>
archive: app.procedure().mutation((&#123; touch &#125;) =&gt; &#123;
  touch(Task, id); <span class="c">/* … */</span>
&#125;)</code></pre>
        </figure>
      </article>
    </section>

    <section class="batteries shell" aria-labelledby="batteries-title">
      <div class="section-heading">
        <p class="section-label">Framework defaults</p>
        <h2 id="batteries-title">Infrastructure failures have default owners.</h2>
        <p class="section-lede">The library defines standard server and client failures and provides React owners for them. Applications add only the layers whose guarantees and reactions depend on product policy.</p>
      </div>

      <div class="ledger">
        <div class="ledger-col">
          <h3><span class="sign plus">+</span> The call path accumulates</h3>
          <ul>
            <li>
              <p class="ledger-src">application procedures and middleware</p>
              <p class="ledger-tags"><code>doc/not-found</code> <code>auth/write-access-required</code></p>
            </li>
            <li>
              <p class="ledger-src">the server boundary</p>
              <p class="ledger-tags"><code>server/bad-request</code> <code>server/internal</code></p>
            </li>
            <li>
              <p class="ledger-src">the client boundary</p>
              <p class="ledger-tags"><code>client/offline</code> <code>client/network-failure</code> <code>client/timeout</code> <code>client/http-failure</code> <code>client/protocol-violation</code> <code>client/decode-failure</code> <code>client/stale</code></p>
            </li>
          </ul>
        </div>

        <div class="ledger-col">
          <h3><span class="sign minus">−</span> The component tree discharges</h3>
          <ul>
            <li>
              <p class="ledger-src"><strong>TransportShell</strong> <span class="badge included">included</span></p>
              <p class="ledger-tags"><code>client/offline</code> <code>client/network-failure</code> <code>client/timeout</code></p>
              <p class="ledger-note">Pauses what's beneath it, resumes on reconnect.</p>
            </li>
            <li>
              <p class="ledger-src"><strong>DefectShell</strong> <span class="badge included">included</span></p>
              <p class="ledger-tags"><code>client/http-failure</code> <code>client/protocol-violation</code> <code>client/decode-failure</code> <code>server/bad-request</code> <code>server/internal</code></p>
              <p class="ledger-note">Nothing a component can render a branch for — escalates to the error boundary.</p>
            </li>
            <li>
              <p class="ledger-src"><strong>StaleShell</strong> <span class="badge included">included</span></p>
              <p class="ledger-tags"><code>client/stale</code></p>
              <p class="ledger-note">A deploy left this tab behind. The reload is the fix.</p>
            </li>
            <li>
              <p class="ledger-src"><strong>WriteAccessShell</strong> <span class="badge yours">application</span></p>
              <p class="ledger-tags"><code>auth/write-access-required</code></p>
              <p class="ledger-note">Opens the login dialog and provides writer context to its subtree.</p>
            </li>
          </ul>
        </div>
      </div>

      <div class="ledger-result">
        <p class="ledger-result-label">What reaches your component</p>
        <p class="ledger-result-tag"><code>doc/not-found</code></p>
        <p class="ledger-result-note">Framework failures are handled by stock shells. Write access is handled by one application shell. The component is left with the domain failure it can render.</p>
      </div>

      <p class="batteries-more">Other application shells can own <code>billing/plan-required</code> for an upgrade dialog, <code>workspace/not-found</code> for a route segment, or <code>api/rate-limited</code> for a retry countdown. Shells form an explicit chain, so application claims extend the built-in owners without replacing them. <a href="/concepts/shells/">How shells work →</a></p>
    </section>

    <section class="faq shell" aria-labelledby="faq-title">
      <div class="section-heading">
        <p class="section-label">Reference</p>
        <h2 id="faq-title">Frequently asked questions.</h2>
      </div>
      <div class="faq-list">
        <details class="faq-item">
          <summary><span>Does it work with React Server Components?</span></summary>
          <div class="faq-body">
            <p>Yes. There's a <code>dehydrate</code>/<code>hydrate</code> bridge and a nestable <code>&lt;ResultRpcHydrationBoundary&gt;</code>: prefetch on the server, and the client renders on first paint with zero requests. Hydrated entities are indexed too, so a client mutation patches a server-rendered row in place. There are working examples for Waku, Next.js App Router, and TanStack Start.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>Why <em>Result</em>? Why not just throw?</span></summary>
          <div class="faq-body">
            <p>Anticipated failures that callers can respond to belong in the procedure contract, not in exceptional control flow. <code>Result</code> makes those branches explicit and closed; throwing remains available for unexpected failures, programmer errors, cancellation, and deliberate boundary escalation.</p>
            <p>A tagged error is validated by its codec and reconstructed after crossing the network. The client therefore receives the same <code>Result&lt;T, E&gt;</code> API and tagged-error definitions used by server code.</p>
            <p>Declared failures produce structured Result events. Unexpected server exceptions remain incident signals with private causes and cross the wire only as <code>server/internal</code>.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>How is this different from returning <code>Result</code> from a tRPC procedure?</span></summary>
          <div class="faq-body">
            <p>Returning a <code>Result</code> from tRPC leaves domain errors inside the data envelope while transport and framework errors remain on <code>query.error</code>. result-rpc places application, transport, protocol, and stale-client failures in one closed operation union, then lets React shells subtract the cases owned above a component.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>Is it inspired by GraphQL?</span></summary>
          <div class="faq-body">
            <p>GraphQL is an influence in two narrower ways. Normalized clients inspired entity identity and the partially normalized client cache: keyed models let a returned entity patch its occurrences across cached procedure results, while those results remain the source of truth. Separately, some GraphQL schemas model expected failures as members of a returned union. That is a schema-design pattern, not behavior GraphQL or Graphcache enforces. result-rpc makes those failures explicit in each procedure's <code>Result&lt;T, E&gt;</code>.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>Do I have to model my whole data graph first?</span></summary>
          <div class="faq-body">
            <p>No, and there's no point where a shape has to be classified. Every query starts as a plain <code>wire.object</code> — the same output you'd have written in tRPC. You mint a model later, when you notice the same keyed thing showing up in several places, and only for the fields that are true about it in every query.</p>
            <p><strong>Relationships are never declared.</strong> There's no relation schema to keep in sync — a relationship is just the shape of one query's output, discovered by walking the decoded result. So you compose whatever a screen needs: an entity nested in a one-off wrapper, an aggregate sitting next to a model, a projection with nothing but an <code>id</code> and a rank.</p>
            <p>The curve is incremental in both directions. Under-modeling costs nothing — an unmodeled node behaves exactly like invalidate-and-refetch, which is where you already are today. And there's no demotion event: aggregates never go inside models, so adding one can't break a model you already have.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>Do I have to use Drizzle?</span></summary>
          <div class="faq-body">
            <p>No. Models are plain wire codecs you can hand-write. Drizzle is one optional subpath that <em>derives</em> them from your tables so a column allowlist is the whole entity contract — but nothing requires it.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>Which server framework does it need?</span></summary>
          <div class="faq-body">
            <p>None in particular. The handler is a standard <code>Request → Response</code> function, so it mounts on Hono, Next.js route handlers, Bun, Deno, Cloudflare Workers — anything that speaks the Web platform.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>Is it realtime? Does it sync across clients?</span></summary>
          <div class="faq-body">
            <p>Subscriptions push server→client over SSE, and the entity cache patches from those events. But there's no peer-to-peer sync and no authoritative stream fanned out to every client — that's a different product (look at Rocicorp's Zero). This is one client, kept incrementally in shape on TanStack Query core.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>Can I keep my Zod / Valibot schemas? And file uploads?</span></summary>
          <div class="faq-body">
            <p>Validators plug in through Standard Schema, for inputs and for forms. Binaries stay out of band on purpose: upload to object storage (a presigned <code>PUT</code>) and put a bucket reference on the contract — which keeps the wire small and the CSRF surface uniform.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary><span>Is it production-ready?</span></summary>
          <div class="faq-body">
            <p>Not yet. The repository is at <code>0.1.0</code>, the package is not published to npm, and the API surface can still move. The implementation is exercised by runnable examples and end-to-end tests, but it should currently be evaluated from source rather than treated as a production dependency.</p>
          </div>
        </details>
      </div>
    </section>

    <section class="cta">
      <div class="shell cta-inner">
        <div>
          <p class="section-label">Coming from tRPC?</p>
          <h2>Migrate per router.<br />Start with the auth layer.</h2>
        </div>
        <div class="cta-actions">
          <a class="button primary light" href="/guides/migrating-from-trpc/">Read the migration guide <span>→</span></a>
          <a href="/reference/examples/">Browse the examples ladder</a>
        </div>
      </div>
    </section>
  </div>

  <footer class="shell" slot="footer">
    <a class="brand" href="/" aria-label="result-rpc home"><img src="/result-rpc-lockup-blue.svg" alt="result-rpc" width="174" height="35" /></a>
    <p>Errors accumulate along the call path and discharge along the component tree.</p>
    <div><a href="/reference/ticket-demo/">Ticket demo</a><a href="/reference/sharp-edges/">Sharp edges</a><a href="https://github.com/jokull/result-rpc">GitHub</a></div>
  </footer>
</PageLayout>

<style>
  :global(*) { box-sizing: border-box; }
  :global(html) { scroll-behavior: smooth; }
  :global(body) { margin: 0; overflow-x: clip; color: #111111; background: #f6f1e7; font-family: var(--blume-font-body); }
  :global([data-blume-header]) { display: none; }
  :global(a) { color: inherit; }
  .shell { width: min(1200px, calc(100% - 48px)); margin-inline: auto; }
  .nav { height: 92px; display: flex; align-items: center; justify-content: space-between; border-bottom: 2px solid #2e5090; }
  .brand { display: inline-flex; align-items: center; text-decoration: none; }
  .brand img { display: block; width: 224px; height: auto; }
  .nav nav { display: flex; align-items: center; gap: 30px; font-size: 14px; font-weight: 500; }
  .nav nav a { text-decoration: none; color: #3f4644; }
  .nav nav a:hover { color: #2e5090; }
  .nav nav .github { padding-left: 29px; border-left: 1px solid rgba(17,17,17,.2); color: #111111; font-weight: 600; }
  .nav nav .github span { color: #2e5090; }
  .hero { display: grid; grid-template-columns: 1.08fr .92fr; gap: 58px; align-items: center; min-height: 650px; padding-block: 76px 88px; }
  .kicker, .section-label, .eyebrow { color: #2e5090; font: 600 11px/1.3 var(--blume-font-mono); letter-spacing: .12em; text-transform: uppercase; }
  .kicker { display: flex; align-items: center; gap: 12px; }
  .kicker span { width: 34px; height: 3px; background: #e7d28a; }
  h1 { margin: 25px 0; color: #111111; font: 700 clamp(55px, 5.6vw, 82px)/.96 var(--blume-font-display); letter-spacing: -.058em; }
  h1 em { color: #2e5090; font-style: normal; font-weight: 500; }
  .lede { max-width: 610px; margin: 0; color: #3f4846; font-size: 18px; line-height: 1.68; }
  .actions { display: flex; flex-wrap: wrap; align-items: center; gap: 14px; margin-top: 34px; }
  .button { min-height: 50px; display: inline-flex; justify-content: center; align-items: center; gap: 16px; padding: 0 21px; border: 1px solid rgba(17,17,17,.24); border-radius: 5px; text-decoration: none; font-size: 14px; font-weight: 600; }
  .button.primary { color: #fffdf7; background: #2e5090; border-color: #2e5090; }
  .button.primary:hover { color: #fffdf7; background: #213e76; border-color: #213e76; }
  .button.secondary { color: #111111; background: #fffdf7; }
  .button.secondary:hover { color: #2e5090; border-color: #2e5090; }
  .hero-visual { min-width: 0; }
  .hero-code { width: 100%; margin: 0; overflow: hidden; border: 1px solid #111111; border-radius: 13px; color: #f6f1e7; background: #111111; box-shadow: -13px 16px 0 #e7d28a; transform: rotate(1deg); }
  .hero-code pre { margin: 0; padding: 28px; overflow-x: auto; font: 400 13px/1.72 var(--blume-font-mono); }
  .hero-code .c { color: #a8c5d9; }
  .hero-code .k { color: #e7d28a; }
  .hero-code figcaption { padding: 15px 28px; border-top: 1px solid #303030; color: #c1d6e3; font-size: 12px; line-height: 1.55; }
  .section-heading { margin-bottom: 44px; }
  .section-heading h2, .cta h2 { margin: 12px 0 0; font: 700 clamp(34px, 3.8vw, 50px)/1.02 var(--blume-font-display); letter-spacing: -.05em; }

  /* --- Four pillars, zigzag rhythm --------------------------------------- */
  .pillars { padding-block: 40px 104px; }
  .pillar { display: grid; grid-template-columns: 1fr 1fr; gap: 56px; align-items: center; padding-block: 54px; border-top: 1px solid rgba(17,17,17,.14); }
  .pillar:first-of-type { border-top: 0; }
  /* zigzag: on even rows the code takes column 1, copy column 2 */
  .pillar:nth-child(even) .pillar-copy { order: 2; }
  .pillar:nth-child(even) .pillar-code { order: 1; }
  .pillar-copy h3 { margin: 14px 0 18px; color: #111111; font: 700 clamp(28px, 3vw, 38px)/1.04 var(--blume-font-display); letter-spacing: -.04em; }
  .pillar-copy p { margin: 0 0 16px; color: #3f4846; font-size: 16px; line-height: 1.66; }
  .pillar-copy p em { color: #2e5090; font-style: normal; font-weight: 600; }
  .pillar-copy p strong { color: #111111; font-weight: 700; }
  .pillar-copy code { padding: .08em .35em; border-radius: 4px; background: rgba(46,80,144,.1); color: #213e76; font: 500 .86em/1 var(--blume-font-mono); }
  .pillar-link { display: inline-flex; align-items: center; gap: 9px; margin-top: 4px; color: #2e5090; font-size: 14px; font-weight: 600; text-decoration: none; }
  .pillar-link span { transition: transform .15s ease; }
  .pillar-link:hover span { transform: translateX(4px); }
  .pillar-code { margin: 0; overflow: hidden; border: 1px solid #111111; border-radius: 12px; background: #111111; box-shadow: -11px 14px 0 #e7d28a; }
  .pillar:nth-child(even) .pillar-code { box-shadow: 11px 14px 0 #a8c5d9; }
  .pillar-code pre { margin: 0; padding: 26px; overflow-x: auto; font: 400 12.5px/1.7 var(--blume-font-mono); color: #f6f1e7; }
  .pillar-code .c { color: #a8c5d9; }
  .pillar-code .k { color: #e7d28a; }
  .pillar-code .s { color: #cdb96a; }
  /* --- Batteries: the add/subtract ledger --------------------------------- */
  .batteries { padding-block: 20px 96px; }
  .section-lede { max-width: 66ch; margin: 18px 0 0; color: #3f4846; font-size: 16.5px; line-height: 1.66; }
  .ledger { display: grid; grid-template-columns: 1fr 1fr; gap: 22px; align-items: start; }
  .ledger-col { padding: 26px; border: 1px solid rgba(17,17,17,.16); border-radius: 9px; background: rgba(255,253,247,.6); }
  .ledger-col:last-child { background: #e4edf4; }
  .ledger-col h3 { display: flex; align-items: center; gap: 11px; margin: 0 0 20px; font: 600 17px/1.2 var(--blume-font-display); letter-spacing: -.02em; }
  .sign { display: inline-flex; align-items: center; justify-content: center; width: 24px; height: 24px; border-radius: 5px; font: 700 15px/1 var(--blume-font-mono); }
  .sign.plus { color: #fffdf7; background: #2e5090; }
  .sign.minus { color: #111111; background: #e7d28a; }
  .ledger-col ul { margin: 0; padding: 0; list-style: none; display: grid; gap: 17px; }
  .ledger-col li { padding-bottom: 17px; border-bottom: 1px dashed rgba(17,17,17,.16); }
  .ledger-col li:last-child { padding-bottom: 0; border-bottom: 0; }
  .ledger-src { margin: 0 0 8px; color: #111111; font-size: 14px; line-height: 1.45; }
  .ledger-src strong { font-weight: 700; }
  .ledger-tags { display: flex; flex-wrap: wrap; gap: 6px; margin: 0; }
  .ledger-tags code, .ledger-src code { padding: .16em .42em; border-radius: 4px; background: rgba(46,80,144,.11); color: #213e76; font: 500 11.5px/1.35 var(--blume-font-mono); }
  .ledger-note { margin: 9px 0 0; color: #4a5351; font-size: 13px; line-height: 1.55; }
  .badge { padding: .18em .5em; border-radius: 4px; font: 600 10px/1 var(--blume-font-mono); letter-spacing: .06em; text-transform: uppercase; }
  .badge.included { color: #1f5133; background: #cfe6d6; }
  .badge.yours { color: #fffdf7; background: #2e5090; }
  .ledger-result { margin-top: 22px; padding: 26px 30px; border: 2px solid #2e5090; border-radius: 9px; display: flex; flex-wrap: wrap; align-items: baseline; gap: 8px 18px; }
  .ledger-result-label { margin: 0; color: #2e5090; font: 600 11px/1.3 var(--blume-font-mono); letter-spacing: .12em; text-transform: uppercase; }
  .ledger-result-tag { margin: 0; }
  .ledger-result-tag code { padding: .2em .5em; border-radius: 5px; background: #2e5090; color: #fffdf7; font: 600 15px/1.3 var(--blume-font-mono); }
  .ledger-result-note { flex: 1 1 380px; margin: 0; color: #3f4846; font-size: 14.5px; line-height: 1.6; }
  .batteries-more { max-width: 78ch; margin: 26px 0 0; color: #4a5351; font-size: 14.5px; line-height: 1.68; }
  .batteries-more code { padding: .08em .35em; border-radius: 4px; background: rgba(46,80,144,.1); color: #213e76; font: 500 .88em/1 var(--blume-font-mono); }
  .batteries-more a { color: #2e5090; font-weight: 600; text-decoration: none; white-space: nowrap; }

  /* --- FAQ ---------------------------------------------------------------- */
  .faq { padding-block: 20px 104px; }
  .faq-list { display: grid; gap: 0; border-top: 1px solid rgba(17,17,17,.16); }
  .faq-item { border-bottom: 1px solid rgba(17,17,17,.16); }
  .faq-item summary { display: flex; align-items: center; justify-content: space-between; gap: 24px; padding: 22px 4px; cursor: pointer; list-style: none; color: #111111; font: 600 clamp(17px, 1.7vw, 20px)/1.35 var(--blume-font-display); letter-spacing: -.02em; }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-item summary > span { display: block; }
  .faq-item summary::after { flex: none; width: 13px; height: 13px; border-right: 2px solid #2e5090; border-bottom: 2px solid #2e5090; transform: rotate(45deg) translate(-3px, -3px); transition: transform .18s ease; content: ""; }
  .faq-item[open] summary::after { transform: rotate(225deg) translate(-3px, -3px); }
  .faq-item summary:hover { color: #2e5090; }
  .faq-item summary em { color: #2e5090; font-style: normal; }
  .faq-item summary code { padding: .08em .34em; border-radius: 4px; background: rgba(46,80,144,.1); color: #213e76; font: 500 .82em/1 var(--blume-font-mono); }
  .faq-body { padding: 0 4px 26px; max-width: 78ch; }
  .faq-body p { margin: 0 0 13px; color: #3f4846; font-size: 15.5px; line-height: 1.68; }
  .faq-body p:last-child { margin-bottom: 0; }
  .faq-body strong { color: #111111; font-weight: 700; }
  .faq-body em { color: #2e5090; font-style: normal; font-weight: 600; }
  .faq-body code { padding: .08em .35em; border-radius: 4px; background: rgba(46,80,144,.1); color: #213e76; font: 500 .86em/1 var(--blume-font-mono); }
  .cta { position: relative; overflow: hidden; color: #fffdf7; background: #2e5090; }
  .cta::after { position: absolute; inset: -65% -8% auto 55%; width: 560px; aspect-ratio: 1; border: 1px solid rgba(255,255,255,.18); border-radius: 50%; box-shadow: 0 0 0 48px rgba(255,255,255,.045), 0 0 0 96px rgba(255,255,255,.035); content: ""; }
  .cta-inner { position: relative; z-index: 1; padding-block: 86px; display: flex; flex-wrap: wrap; gap: 40px; align-items: center; justify-content: space-between; }
  .cta .section-label { color: #e7d28a; }
  .cta-actions { display: flex; align-items: center; gap: 26px; }
  .cta-actions > a:last-child { color: #dce8f0; font-size: 14px; }
  .button.primary.light { color: #21304b; background: #fffdf7; border-color: #fffdf7; }
  .button.primary.light:hover { color: #111111; background: #e7d28a; border-color: #e7d28a; }
  footer.shell { display: flex; flex-wrap: wrap; gap: 20px; align-items: center; justify-content: space-between; padding-block: 42px; color: #4a5351; font-size: 13px; }
  footer .brand img { width: 174px; }
  footer div { display: flex; gap: 22px; }
  footer a { text-decoration: none; }
  @media (max-width: 900px) {
    .hero { grid-template-columns: 1fr; min-height: 0; gap: 54px; padding-block: 55px 76px; }
    .hero-code { max-width: 680px; }
    /* stack pillars: copy first, code after, on every row */
    .pillar { grid-template-columns: 1fr; gap: 32px; padding-block: 44px; }
    .pillar:nth-child(even) .pillar-copy,
    .pillar:nth-child(even) .pillar-code { order: 0; }
    .pillar:nth-child(even) .pillar-code { box-shadow: -11px 14px 0 #e7d28a; }
    .ledger { grid-template-columns: 1fr; }
  }
  @media (max-width: 680px) {
    .shell { width: min(100% - 30px, 1200px); }
    .nav { height: 78px; }
    .brand img { width: 184px; }
    .nav nav { gap: 16px; font-size: 12px; }
    .nav nav a:nth-child(2), .nav nav a:nth-child(3), .nav nav a:nth-child(4) { display: none; }
    .nav nav .github { padding-left: 16px; }
    h1 { font-size: clamp(47px, 14vw, 66px); }
    .lede { font-size: 16px; }
    .hero-code { width: calc(100% - 10px); }
    .hero-code pre, .pillar-code pre { padding: 22px 18px; font-size: 11px; }
    .faq-item summary { padding: 19px 2px; gap: 16px; }
    .faq-body { padding: 0 2px 22px; }
    .pillar-copy h3 { font-size: clamp(25px, 8vw, 32px); }
    .cta-inner, .cta-actions { align-items: flex-start; flex-direction: column; }
  }
</style>
